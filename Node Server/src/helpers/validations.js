const User = require('../../models/User');

const requiredFields = registerFields => {
  // check all fields
};

const validateEmailAndUsername = (email, username) => {
  // check if email or username has been used in db
};

const validatePasswords = (passwordOne, passwordTwo) => {
  // make sure passwords are identical
  // todo check password rules
};
